import 'package:flutter/material.dart';

class DescriptionPageAni extends StatelessWidget {
  const DescriptionPageAni({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: CustomScrollView(
        slivers: [
          SliverAppBar(
          )
        ],
      ),
    );
  }
}
